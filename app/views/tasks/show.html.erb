<br><br><h1 class="text-center">Task Details</h1><br>

<div class="d-flex justify-content-center flex-column align-items-center w-50 mx-auto">
  <div class="mb-2 w-100">
    <%= button_tag @task.title, class: 'btn btn-dark w-100 text-start' %>
  </div>

  <div class="mb-2 w-100">
    <%= button_tag @task.details, class: 'btn btn-light w-100 text-start' %>
  </div>

  <div class="mb-2 w-100">
    <%= button_tag @task.completed ? "Completed" : "To Do", class: "btn #{@task.completed ? 'btn-success' : 'btn-danger'} w-100 text-start" %>
  </div>

<div class="mt-3 d-flex justify-content-between w-50">
  <%= link_to 'Edit', edit_path(@task), class: 'btn btn-warning text-center' %>
  <%= link_to 'Delete', destroy_path(@task), data: {turbo_method: :delete}, class: 'btn btn-danger text-center' %>
  <%= link_to 'Back to Tasks', list_path, class: 'btn btn-secondary text-center' %>
</div>

</div>
